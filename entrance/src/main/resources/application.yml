server:
  port: 8899
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/electron?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 200309
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB
  profiles:
    active: common,mapper,service # 指定配置文件
  data:
    redis:
      port: 6379
      host: 127.0.0.1
      password:

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true # 开启驼峰命名
jwt:
  key: ${JWT_KEY}

minio:
  endpoint: ${ENDPOINT}
  accessKey: ${ACCESSKEY}
  secretKey: ${SECRETKEY}
  uploadKey: ${UPLOADKEY}
  userAvatar: useravatar
  downloadApp: downloadapp
  image: image
